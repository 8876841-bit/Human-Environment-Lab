# Human-Environment-Lab 交接说明

> 本文件用于跨工具、跨会话接续。进入新会话后，先读本文件，再读 `CURRENT-MAINLINE.md`、`RUNBOOK.md` 和 `05-内容生产体系/00-内容生产硬校验清单.md`。

---

## 当前任务状态

仓库已进入：

```text
V2.2：内容生产体系硬校验 + TP 样品验证阶段
```

当前重点不是继续扩展新模块，而是：

```text
真实触发点
↓
第一性原理锚点
↓
平台与表达前置校验
↓
内容形式决策
↓
图文 / 视频成品包
↓
发布
↓
真实数据复盘
↓
有长期价值再沉淀为 OBS
```

---

## 当前必须先读的 6 个文件

```text
CURRENT-MAINLINE.md
RUNBOOK.md
05-内容生产体系/00-内容生产硬校验清单.md
05-内容生产体系/10-Dify工作流搭建实施指南.md
05-内容生产体系/16-平台规则与发布调性表.md
05-内容生产体系/17-第一性原理到平台表达转译表.md
```

这 6 个文件决定当前主线。

---

## 当前主线文件

当前唯一执行主线：

```text
05-内容生产体系/10-Dify工作流搭建实施指南.md
```

当前硬校验入口：

```text
05-内容生产体系/00-内容生产硬校验清单.md
```

当前平台适配入口：

```text
05-内容生产体系/16-平台规则与发布调性表.md
```

当前转译入口：

```text
05-内容生产体系/17-第一性原理到平台表达转译表.md
```

当前一致性审计入口：

```text
05-内容生产体系/18-内容生产体系一致性审计.md
```

---

## 当前已经完成的治理动作

1. 新增 `CURRENT-MAINLINE.md`：声明当前主线。
2. 新增 `STATUS.md`：标记各目录状态。
3. 新增 `DECISION-LOG.md`：记录重大决策。
4. 新增并更新 `RUNBOOK.md`：日常运行手册，已同步 v4.1 硬校验。
5. 更新 `README.md`：指向当前主线。
6. 给历史目录增加 README，标注为 LEGACY / EXPERIMENT。
7. 明确 OBS 不等于装修案例，八条母线都可以作为真实触发点入口。
8. 新增 `05-内容生产体系/00-内容生产硬校验清单.md`。
9. 将 `10-Dify工作流搭建实施指南.md` 升级为 v4.1。
10. 新增 `14-图文视觉规范-决策板风格.md`。
11. 新增 `15-平台适配与表达转译前置校验.md`。
12. 新增 `16-平台规则与发布调性表.md`。
13. 新增 `17-第一性原理到平台表达转译表.md`。
14. 新增 `18-内容生产体系一致性审计.md`。
15. 更新 `11-内容形式决策器.md`，同步一题多形规则、平台调性、第一性原理锚点。
16. 更新 `12-图文成品形式库.md`，同步平台、受众、S1/S2/S3 边界和成品包标准。
17. 更新 `13-视频成品形式库.md`，同步视频时长不固定、V1/V2/V3 成品边界和平台风险。
18. 更新 TP-001 样例，确认图文样式 V1 和视频执行清单 V1。

---

## 当前模块状态

| 模块 | 状态 | 说明 |
|---|---|---|
| `01-观察框架/` | FOUNDATION | 底层理论框架 |
| `02-内容引擎/` | FOUNDATION | 早期内容流水线 V1 |
| `03-观察日志/` | CORE | 用于沉淀发布后验证过的长期观察 |
| `05-内容生产体系/` | CORE | 当前内容体系主线，已建立硬校验与形式库 |
| `06-Dify多Agent工作流/` | CORE / NEEDS_TEST | Dify DSL 与知识库资产，需实测联调 |
| `guide/` | LEGACY | 早期 Coze/Liblib/TapNow 指南 |
| `04-账号矩阵/` | LEGACY / LONG-TERM | 中长期设想，当前不铺 8 个账号 |
| `agent-studio/` | EXPERIMENT | 浏览器端实验工作台 |
| `hel-agent-workbench/` | EXPERIMENT | Python Agent 原型 |
| `04-外部输入模块/` | PLANNING | n8n 外部输入，二期再做 |

---

## 当前最重要的下一步

不是继续扩新框架，而是继续跑 TP 样品。

优先顺序：

```text
1. 用 00 硬校验清单检查 TP-001。
2. 用 16 平台规则确定 TP-001 首发版本。
3. 用 17 转译表确认第一性原理锚点和平台表达。
4. 继续 TP-001 视频关键画面素材测试。
5. 发布后记录真实数据。
6. 再决定是否沉淀为 OBS。
```

TP-001 当前判断：

```text
触发点：AI 工具越多，我反而越不知道怎么开始。
第一性原理锚点：工具过载会破坏普通人的判断顺序。
主形式：V2 AI 生成型短视频。
衍生形式：小红书 5 页收藏卡。
```

---

## 当前不要做的事

```text
不要把 HEL 收窄成装修号。
不要继续扩 8 个账号。
不要继续新增视觉形式。
不要继续堆更多 Agent 名字。
不要急着做 n8n 全平台采集。
不要把 agent-studio 公开部署。
不要把系统包装成已经全自动成片。
不要把平台热词当成底层判断。
不要把底层术语直接端到平台上讲。
不要把没有发布的数据当成验证。
```

---

## 新会话第一句话建议

```text
请先阅读 HANDOFF.md、CURRENT-MAINLINE.md、RUNBOOK.md、05-内容生产体系/00-内容生产硬校验清单.md、10-Dify工作流搭建实施指南.md、16-平台规则与发布调性表.md、17-第一性原理到平台表达转译表.md，然后继续推进 TP-001 视频关键画面素材测试。
```

---

## 当前判断

HEL 当前不是缺更多框架，而是缺真实运行。

下一阶段只看三个指标：

```text
1. 是否把真实触发点做成了可发布成品？
2. 是否发布并获得真实数据？
3. 是否从真实数据里反向生成新的触发点或 OBS？
```
