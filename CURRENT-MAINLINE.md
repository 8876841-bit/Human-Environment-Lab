# CURRENT MAINLINE（当前主线）

> 本文件用于声明 Human-Environment-Lab 当前执行主线，避免旧方案、新方案、实验工具、内容流程互相干扰。

---

## 0. 当前版本

```text
V3.0：碳基—硅基双分身 Human-Environment-Lab
```

V3 的主线不是单纯内容生产，而是：

```text
碳基真实输入
→ 硅基数字处理
→ Human-Environment-Lab 转译与研究
→ 表达 / 传播 / 工具 / 产品 / 服务 / 行动 / 暂不表达
→ 物理世界验证
→ 数字生命与 Lab 双重迭代
```

原 V2 内容生产体系不删除，但降级为：

```text
Human-Environment-Lab 的表达与传播模块。
```

---

## 1. 顶层关系

### 1.1 碳基的我

我主打物理世界的真实。

我负责：

```text
进入现实
感受现实
接触真实的人、事、关系、环境、工具和系统
带回真实触发、身体反应、现场材料、行动反馈和现实后果
做价值选择
划定伦理边界
承担最终责任
```

### 1.2 硅基数字生命

数字生命主打数字世界的全部能力。

它负责：

```text
长期记忆
整理材料
分析结构
建模推演
连接跨领域信息
生成假设
提出反问
识别模式
设计验证路径
表达转译
边界把控
复盘反馈
更新规则
```

### 1.3 Human-Environment-Lab

Human-Environment-Lab 是碳基真实与硅基能力之间的转换实验室。

它负责：

```text
把物理世界的真实触发转成可研究对象
把真实材料转成可追问问题
把问题转成可验证假设
把硅基分析转成可理解表达
把数字推演转成可回到物理世界的交付形式
把现实反馈转回数字生命与 Lab 的规则系统
```

---

## 2. 当前唯一顶层主线文件

当前顶层内核以以下文件为准：

```text
00-系统内核/00-碳基硅基双分身协作协议.md
00-系统内核/01-Human-Environment-Lab总定义.md
00-系统内核/02-Loop-Engineering运行协议.md
00-系统内核/03-迁移说明-V2到V3.md
```

当前执行入口：

```text
RUNBOOK.md
```

当前状态地图：

```text
STATUS.md
```

---

## 3. 触发点优先，OBS 降级

HEL 的核心输入仍然不是完整 OBS，而是一个真实触发点。

```text
触发点 = 一句话真实感受 / 一个问题 / 一个判断 / 一个现象 / 一次行动反馈 / 一次关系变化
OBS = 对值得复用、值得追踪、被验证过的触发点进行归档后的观察日志
```

但 V3 修正了触发点的流向。

V2 流向：

```text
触发点 → 选题 → 脚本 → 发布 → 复盘
```

V3 流向：

```text
触发点
↓
数字生命整理、分析、建模、推演、边界把控
↓
Lab 判断：研究 / 表达 / 内容 / 工具 / 产品 / 服务 / 行动 / 暂不表达
↓
物理世界验证
↓
双重迭代
```

只有当触发点被现实反馈证明有长期价值时，才沉淀为 OBS 或规则。

---

## 4. 重要边界：HEL 不等于装修号，也不等于内容号

HEL 不以任何单一行业为边界。

```text
装修 / 家装 / 工艺 / 柜体
只是旧仓库素材较多、验证成本较低的一类入口。
它不是 HEL 的边界。
```

HEL 也不等于内容生产系统。

```text
内容只是 Human-Environment-Lab 的一种交付形式。
```

真实触发点可以来自：

```text
AI 学习与工具使用
真实生活与身份转型
商业选择与判断系统
关系、平台、技术、社会规则
身体感受和行动失败
意义、文明、真实生活
已有空间、材料、工艺等旧资产
```

当前阶段选择一个触发点，只看四个条件：

```text
1. 它是否来自真实感受、真实观察、真实关系或真实反馈。
2. 它能否被数字生命做深加工。
3. 它能否通过 Lab 形成某种交付。
4. 它最终能否回到物理世界验证。
```

---

## 5. 当前执行主线

```text
1. 输入一个真实触发点
2. 判断它是否属于碳基真实材料
3. 数字生命进行硅基处理：整理 / 分析 / 建模 / 推演 / 边界把控
4. Human-Environment-Lab 判断交付形式
5. 选择：内部研究 / 公开表达 / 内容传播 / 工具 / 产品 / 服务 / 行动 / 暂不表达
6. 碳基的我带回物理世界验证
7. 记录现实反馈
8. 更新数字生命记忆、Lab 规则或运行日志
```

当前不再默认进入：

```text
选题 → 脚本 → 分镜 → 发布
```

只有当 Lab 判断“内容传播”是合适交付形式时，才调用：

```text
05-内容生产体系/
06-Dify多Agent工作流/
```

---

## 6. V2 内容生产体系的当前定位

`05-内容生产体系/` 仍然保留。

但它的状态从旧主线调整为：

```text
OUTPUT：表达与传播模块
```

它适用于：

```text
需要公开表达
需要平台传播
需要图文 / 视频成品包
需要 Dify 节点式处理
需要发布后复盘
```

它不适用于：

```text
尚未验证的宏大判断
不适合公开的真实材料
需要先做现实实验的想法
应该沉默或暂存的触发点
```

---

## 7. Dify 的当前定位

Dify 仍然是长期底座之一，但它不再代表整个 HEL。

当前定位：

```text
Dify = 表达与传播模块的长期工作流底座
```

下一步需要同步：

```text
06-Dify多Agent工作流/
```

同步目标：

```text
把 Dify 从“内容生产工作流”升级为：
真实触发 → 硅基处理 → Lab 交付判断 → 表达传播执行包。
```

在同步前，Dify DSL 标记为：

```text
NEEDS_SYNC
```

---

## 8. 当前阶段目标

当前不是继续扩新框架，也不是马上重写全部旧文件。

当前目标是：

```text
用已有真实内容跑通 3 次 V3 Loop。
```

最小验收：

```text
1. 整理 10 条已有真实触发点。
2. 从中选择 3 条进入 Human-Environment-Lab 运行日志。
3. 每条都完成：碳基输入 → 硅基处理 → Lab 交付判断。
4. 至少 1 条带回物理世界验证。
5. 至少 1 条产生规则更新或明确废弃原因。
```

---

## 9. 当前禁止事项

```text
不要把 HEL 收窄成装修号。
不要把 HEL 简化成内容工厂。
不要默认每个触发点都要发内容。
不要把数字生命当成普通文案助手。
不要让数字生命伪装成有身体体验。
不要把没有现实反馈的推演写进稳定规则库。
不要继续新增 Agent 名字。
不要急着做 n8n 全平台采集。
不要把 agent-studio 公开部署。
不要把系统包装成已经全自动运行。
```

当前原则：

```text
先保护碳基真实。
再调用硅基能力。
再由 Lab 判断交付形式。
最后必须回到物理世界验证。
```

---

*最后更新：2026-06-16*