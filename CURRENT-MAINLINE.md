# CURRENT MAINLINE（当前主线）

> 本文件用于声明 Human-Environment-Lab 当前执行主线，避免旧方案、新方案、实验工具互相干扰。

---

## 0. 重要边界：触发点优先，OBS 降级

HEL 的核心输入不是完整 OBS，而是：

```text
底层逻辑 / 第一性原理 / 八条母线
+
一个当前触发点
```

OBS 不是强制输入格式。OBS 只用于保存值得长期复用的真实观察。

```text
触发点 = 一句话真实感受 / 一个问题 / 一个判断 / 一个现象
OBS = 对值得复用的触发点进行归档后的观察日志
```

例子：

```text
AI 工具越多，我反而越不知道怎么开始。
```

这一句话就可以进入内容生产。只有当它值得沉淀、复盘、反复调用时，再扩展为 OBS 文件。

---

## 1. 重要边界：HEL 不等于装修号

HEL 有八条母线，当前主线不要求从装修开始。

```text
装修 / 家装 / 工艺 / 柜体
只是已有素材较多、验证成本较低的一类入口。
它不是 HEL 的边界。
```

真实触发点可以来自八条母线中的任意一条：

| 母线 | 可投喂的触发点示例 |
|---|---|
| 空间秩序 | 家里越住越乱、玄关堆满东西、餐桌变成杂物中转站 |
| 材料感官 | 木头让人放松、不锈钢让人觉得干净、灯光让空间压抑 |
| 工艺机制 | 柜子收口、尺寸偏差、安装责任、交付翻车 |
| 案例探家 | 一个家的布局暴露了家庭运行方式、独居者如何安排空间 |
| AI 认知秩序 | AI 工具越装越多反而更乱、不会判断模型输出 |
| 真实生活转型 | 旧行业失效、工作室停滞、想换活法但不知道从哪开始 |
| 商业服务 | 全屋定制责任边界、价格透明、客户真正缺的是判断系统 |
| 意义与文明 | AI 时代真实生活为什么更稀缺、人还需要什么意义 |

当前阶段选择某个触发点，不看它是不是装修，只看三个条件：

```text
1. 它是真实感受 / 真实判断 / 真实现象。
2. 它能被你用一句话说清楚。
3. 它能进入内容发布验证。
```

---

## 2. 当前执行主线

```text
底层模型 / 八条母线
+
一句触发点
↓
AI 推演：所属母线 / 核心机制 / 用户痛点
↓
选题生成
↓
人工确认选题
↓
结构化脚本生成
↓
人工修改 Hook 与金句
↓
视觉 / 分镜 / 执行清单
↓
人工剪辑与发布
↓
发布复盘
↓
有长期价值的内容，再沉淀成 OBS
```

当前唯一主线文件：

```text
05-内容生产体系/10-Dify工作流搭建实施指南.md
```

当前内容体系总入口：

```text
05-内容生产体系/README.md
```

---

## 3. 长期底座

```text
Dify
```

理由：

- 适合复杂工作流编排。
- 适合挂载知识库。
- 适合长期沉淀个人语料、触发点、OBS 日志、底层模型和复盘数据。
- 适合把 HEL 从“提示词集合”推进到“内容生产系统”。

---

## 4. 快速验证工具

```text
Coze（扣子）
```

定位：

- 只用于快速验证 Bot / Agent 的最小流程。
- 不作为当前长期主线。
- 旧版 `guide/HEL-多Agent内容生产流水线操作指南.md` 归为历史参考。

---

## 5. 实验项目

```text
agent-studio/
hel-agent-workbench/
```

定位：

- `agent-studio/`：浏览器端实验工作台，仅适合本地实验，不建议公网部署。
- `hel-agent-workbench/`：Python 版 Agent 原型，可用于验证流程与代码化能力，但不压过 Dify 主线。

---

## 6. 二期模块

```text
04-外部输入模块/n8n多平台内容采集引擎.md
```

定位：

- 用于后续采集外部趋势、评论痛点、竞品素材。
- 当前阶段暂不作为核心执行任务。
- 必须遵守平台条款、版权、肖像权与素材使用边界。

---

## 7. 当前阶段目标

```text
用 10 个真实触发点跑通：
触发点 → 选题 → 脚本 → 分镜 → 发布 → 复盘。
```

这 10 个触发点不要求来自同一母线，也不要求来自装修。

更推荐的结构是：

```text
2 个来自 AI 学习与工具使用
2 个来自真实生活转型
2 个来自商业服务与判断系统
2 个来自空间 / 材料 / 工艺
1 个来自案例探家
1 个来自意义与文明观察
```

优先级顺序：

1. 收集真实触发点。
2. 用底层模型推演选题。
3. 输出可发布内容包。
4. 实际发布并记录数据。
5. 只有被验证有价值的触发点，再沉淀为 OBS。

---

## 8. 禁止当前阶段继续扩散的事项

```text
不要把 HEL 收窄成装修号。
不要把 OBS 当成强制大表格。
不要同时铺 8 个账号。
不要继续新增视觉形式。
不要继续堆更多 Agent 名字。
不要急着做 n8n 全平台采集。
不要把 agent-studio 公开部署。
不要把系统包装成已经全自动成片。
```

当前原则：

```text
先用一句触发点启动内容。
先稳定生产，再扩大系统。
先真实发布，再谈自动化。
先验证主线，再扩展工具。
先保护八条母线，再选择最低摩擦入口。
```
